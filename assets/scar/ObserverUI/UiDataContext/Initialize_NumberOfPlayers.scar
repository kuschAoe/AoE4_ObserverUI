UiNumberOfPlayers = {}

ObserverUiInitialization:AddInitializable(UiNumberOfPlayers)

function UiNumberOfPlayers:Initialize()
	for i = 8, #PLAYERS + 1, -1 do
		ObserverUIDataContextMappings.PlayersByIndex[i] = nil
	end
end
